<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <title><%= content_for(:title) || "ACE - NITK" %></title>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <style>
      body {
        font-family: 'Arial', sans-serif;
        background-color: #f5f5f5;
        margin: 0;
        padding: 0;
      }

      header {
        background-color: #009688;
        color: white;
        padding: 20px 0;
        text-align: center;
      }

      h1 {
        font-size: 3rem;
        margin: 0;
      }

      p {
        font-size: 1.2rem;
        margin-top: 10px;
      }

      .main-content {
        display: flex;
        justify-content: center;
        align-items: center;
        height: calc(100vh - 120px);
        background-color: #ffffff;
        padding: 20px;
      }

      .card {
        background-color: #fff;
        border-radius: 8px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        padding: 40px;
        max-width: 600px;
        text-align: center;
      }

      .card h2 {
        font-size: 2.5rem;
        margin-bottom: 15px;
      }

      .card p {
        font-size: 1.1rem;
        color: #555;
        margin-bottom: 25px;
      }

      .cta-button {
        background-color: #00796b;
        color: white;
        padding: 12px 24px;
        border: none;
        border-radius: 5px;
        font-size: 1.2rem;
        cursor: pointer;
        transition: background-color 0.3s ease;
      }

      .cta-button:hover {
        background-color: #004d40;
      }

      footer {
        background-color: #009688;
        color: white;
        padding: 15px 0;
        text-align: center;
      }

      footer a {
        color: white;
        text-decoration: none;
        margin: 0 10px;
        font-size: 1.1rem;
      }

      footer a:hover {
        text-decoration: underline;
      }
    </style>
  </head>

  <body>
    <header>
      <h1>Welcome to ACE NITK</h1>
      <p>Association of Computer Engineers, NITK</p>
    </header>

    <div class="main-content">
      <div class="card">
        <h2>Empowering the Next Generation of Engineers</h2>
        <p>Join ACE to be a part of exciting events, workshops, and networking opportunities that will help you grow as an engineer!</p>
        <a href="/join" class="cta-button">Join Us</a>
      </div>
    </div>

    <footer>
      <p>&copy; <%= Time.now.year %> ACE - NITK. All rights reserved.</p>
      <p>
        <a href="/about">About Us</a> |
        <a href="/contact">Contact</a> |
        <a href="/events">Events</a>
      </p>
    </footer>
  </body>
</html>
